---
import Layout from '../../layouts/Layout.astro';

const categories = [
  {
    title: 'Desarrollo',
    items: [
      ['Formateador HTML/CSS/JS', 'Formatea y minifica snippets de código localmente.', '/code-formatter/', 'CODE'],
      ['Conversor de timestamps y zonas horarias', 'Convierte Unix timestamps, fechas ISO y zonas horarias.', '/timestamp-converter/', 'TIME'],
      ['Base64 encoder y decoder', 'Codifica y decodifica Base64.', '/base64/', '64'],
      ['Parser y conversor de CSV', 'Previsualiza CSV y conviértelo a JSON, Markdown o HTML.', '/csv-converter/', 'CSV'],
      ['JSON viewer y formatter', 'Visualiza y valida JSON.', '/json-viewer/', '{}'],
      ['Generador MD5', 'Genera hashes MD5.', '/md5/', 'MD5'],
      ['JWT decoder', 'Decodifica JWT localmente.', '/jwt-decoder/', 'JWT'],
      ['URL encoder & decoder', 'Codifica parámetros y URLs.', '/url-encoder/', 'ENC'],
      ['Inspector de URLs', 'Analiza URLs para desarrolladores.', '/url-inspector/', 'URL'],
      ['Configurador cURL', 'Genera peticiones HTTP.', '/curl-builder/', 'API'],
      ['Diff checker', 'Compara textos rápidamente.', '/diff-checker/', 'DIFF']
    ]
  },
  {
    title: 'Imágenes',
    items: [
      ['Generador de iconos para apps', 'Crea iconos con imagen, textos, bordes y exportación ZIP.', '/app-icon-generator/', 'APP'],
      ['Generador de placeholders SVG', 'Crea SVG, data URLs y snippets HTML/CSS.', '/svg-placeholder/', 'SVG'],
      ['Generador y lector QR', 'Crea códigos QR y lee imágenes locales.', '/qr-code/', 'QR'],
      ['Compresor de imágenes', 'Reduce el tamaño de imágenes.', '/image-compressor/', 'ZIP'],
      ['Conversor de imágenes', 'Convierte PNG, JPEG y WebP.', '/image-converter/', 'CONV'],
      ['Información de imágenes', 'Obtén metadatos y tamaño.', '/image-info/', 'IMG'],
      ['Generador de favicon', 'Crea favicons desde texto o imágenes.', '/favicon-generator/', 'ICO'],
      ['Censurador de fotos', 'Pixelado, blur y censura.', '/photo-censor/', 'CEN']
    ]
  },
  {
    title: 'Internet y mapas',
    items: [
      ['DNS lookup', 'Consulta registros DNS con Google Public DNS.', '/dns-lookup/', 'DNS'],
      ['Información de IP', 'Consulta información técnica de IPs.', '/ip-info/', 'IP'],
      ['Coordenadas de direcciones', 'Busca coordenadas GPS.', '/geocoder/', 'MAP'],
      ['Preview social de URLs', 'Comprueba Open Graph y Twitter.', '/social-preview/', 'SOC']
    ]
  },
  {
    title: 'Texto y escritura',
    items: [
      ['Generador de Lorem Ipsum', 'Genera texto por letras, párrafos o páginas.', '/lorem-ipsum/', 'TXT'],
      ['Markdown writer & preview', 'Editor Markdown con preview.', '/markdown-preview/', 'MD']
    ]
  }
];

const totalTools = categories.reduce((total, category) => total + category.items.length, 0);
---

<Layout
  title="Alon Tools — Herramientas privadas para navegador"
  description="Herramientas rápidas para desarrollo, imágenes e internet que funcionan localmente en tu navegador. Sin backend, sin cuentas y sin subir archivos a la nube."
  lang="es"
  path="/"
  translated={true}
>
  <section class="hero">
    <div class="eyebrow">Privacy First</div>

    <h1>Herramientas rápidas para el navegador que respetan tu privacidad</h1>

    <p class="hero-text">
      Sin backend. Sin cuentas. Sin subir archivos a la nube. La mayoría de herramientas se ejecutan completamente en tu dispositivo.
    </p>
  </section>

  <section class="search-panel" aria-labelledby="toolsSearchTitle">
    <div class="search-heading">
      <div>
        <h2 id="toolsSearchTitle">Buscar herramienta</h2>
        <p id="toolsCount">Mostrando {totalTools} herramientas</p>
      </div>

      <button id="clearFilters" type="button">Limpiar filtros</button>
    </div>

    <label for="toolSearch">Busca por nombre, descripción o etiqueta</label>
    <input id="toolSearch" type="search" placeholder="Buscar: json, imagen, qr, timestamp..." autocomplete="off" />

    <div class="category-filters" aria-label="Filtros por categoría">
      <button class="filter-button active" type="button" data-category-filter="all">Todas</button>
      {categories.map((category) => (
        <button class="filter-button" type="button" data-category-filter={category.title}>{category.title}</button>
      ))}
    </div>
  </section>

  <p class="empty-state" id="emptyState" hidden>No hay herramientas que coincidan con tu búsqueda.</p>

  {
    categories.map((category) => (
      <section class="category-section" data-category-section={category.title}>
        <div class="category-header">
          <h2>{category.title}</h2>
        </div>

        <div class="directory-grid">
          {
            category.items.map(([title, description, href, icon]) => (
              <a
                class="tool-link-card"
                href={href}
                data-tool-card
                data-category={category.title}
                data-search={`${title} ${description} ${href} ${icon} ${category.title}`.toLowerCase()}
              >
                <div class="tool-icon">{icon}</div>

                <div>
                  <strong>{title}</strong>
                  <small>{description}</small>
                </div>
              </a>
            ))
          }
        </div>
      </section>
    ))
  }

  <script>
    const searchInput = document.getElementById('toolSearch');
    const clearFilters = document.getElementById('clearFilters');
    const toolsCount = document.getElementById('toolsCount');
    const emptyState = document.getElementById('emptyState');
    const cards = Array.from(document.querySelectorAll('[data-tool-card]'));
    const sections = Array.from(document.querySelectorAll('[data-category-section]'));
    const filterButtons = Array.from(document.querySelectorAll('[data-category-filter]'));
    const total = cards.length;
    let activeCategory = 'all';

    const setActiveButton = () => {
      filterButtons.forEach((button) => {
        button.classList.toggle('active', button.dataset.categoryFilter === activeCategory);
      });
    };

    const filterTools = () => {
      const query = searchInput.value.trim().toLowerCase();
      let visible = 0;

      cards.forEach((card) => {
        const matchesCategory = activeCategory === 'all' || card.dataset.category === activeCategory;
        const matchesSearch = !query || card.dataset.search.includes(query);
        const show = matchesCategory && matchesSearch;
        card.hidden = !show;
        if (show) visible += 1;
      });

      sections.forEach((section) => {
        section.hidden = !section.querySelector('[data-tool-card]:not([hidden])');
      });

      toolsCount.textContent = `Mostrando ${visible} de ${total} herramientas`;
      emptyState.hidden = visible !== 0;
      setActiveButton();
    };

    filterButtons.forEach((button) => {
      button.addEventListener('click', () => {
        activeCategory = button.dataset.categoryFilter || 'all';
        filterTools();
      });
    });

    searchInput.addEventListener('input', filterTools);
    clearFilters.addEventListener('click', () => {
      searchInput.value = '';
      activeCategory = 'all';
      filterTools();
      searchInput.focus();
    });

    filterTools();
  </script>
</Layout>

<style>
  .hero {
    padding: 4rem 0 2rem;
  }

  .search-panel {
    margin: 1rem 0 2.5rem;
    padding: 1.25rem;
    border-radius: 24px;
    border: 1px solid var(--border);
    background: var(--card);
  }

  .search-heading {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .search-heading h2 {
    margin: 0 0 .25rem;
    font-size: 1.25rem;
  }

  .search-heading p,
  label {
    color: var(--muted);
  }

  .search-heading p {
    margin: 0;
  }

  label {
    display: block;
    margin-bottom: .55rem;
    font-weight: 700;
  }

  input[type='search'] {
    width: 100%;
    border-radius: 16px;
    border: 1px solid var(--border);
    background: #020617;
    color: white;
    padding: 1rem;
    font: inherit;
  }

  .category-filters {
    display: flex;
    gap: .65rem;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  button,
  .filter-button {
    border-radius: 999px;
    border: 1px solid var(--border);
    background: #0f172a;
    color: white;
    padding: .75rem 1rem;
    cursor: pointer;
    font: inherit;
    font-weight: 700;
  }

  .filter-button.active,
  #clearFilters:hover {
    background: var(--primary);
    color: #020617;
    border-color: transparent;
  }

  .empty-state {
    padding: 1.25rem;
    border-radius: 18px;
    border: 1px dashed var(--border);
    color: var(--muted);
    text-align: center;
  }

  .category-section {
    margin-top: 3rem;
  }

  .category-section[hidden] {
    display: none;
  }

  .category-header {
    margin-bottom: 1rem;
  }

  .category-header h2 {
    margin: 0;
    font-size: 1.4rem;
  }

  .directory-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  }

  .tool-link-card {
    display: flex;
    gap: 1rem;
    padding: 1.5rem;
    border-radius: 24px;
    border: 1px solid var(--border);
    background: var(--card);
    color: inherit;
    text-decoration: none;
    transition: transform .15s ease, border-color .15s ease;
  }

  .tool-link-card:hover {
    transform: translateY(-2px);
    border-color: rgba(56,189,248,.35);
  }

  .tool-link-card[hidden] {
    display: none;
  }

  .tool-icon {
    width: 64px;
    height: 64px;
    display: grid;
    place-items: center;
    border-radius: 18px;
    background: var(--primary);
    color: black;
    font-weight: 900;
    font-size: .9rem;
    flex-shrink: 0;
  }

  .tool-link-card strong {
    display: block;
    margin-bottom: .5rem;
  }

  .tool-link-card small {
    color: var(--muted);
    line-height: 1.7;
  }

  @media (max-width: 640px) {
    .hero {
      padding-top: 2.5rem;
    }

    .search-heading {
      flex-direction: column;
    }

    #clearFilters,
    .filter-button {
      width: 100%;
    }
  }
</style>
